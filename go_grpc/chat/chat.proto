/***
 * protoc --go_out=. *.proto
 * protoc --go-grpc_out=. chat/chat.proto 
 */

syntax="proto3";

option go_package = "chat/";

message Person {
  string name = 1;
  int32 age = 2;
}

message Message {
  string body = 1;
}

service ChatService {
  rpc SayHello(Message) returns (Message) {}
}
